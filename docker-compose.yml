services:
  db:
    image: postgres:16.3
    container_name: go-social-postgres-db
    environment:
      POSTGRES_DB: go_social
      POSTGRES_USER: aok
      POSTGRES_PASSWORD: aokpassword
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  db-data:
